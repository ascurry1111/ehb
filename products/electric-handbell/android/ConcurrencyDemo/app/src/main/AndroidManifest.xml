<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLUETOOTH_ADVERTISE is the one this app is really about: the start
         beacon requires peripheral/advertiser role, which is a separate
         capability from scanning and not present on every device. -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- neverForLocation: we only ever match our own manufacturer data, never
         derive location from BLE, so no ACCESS_FINE_LOCATION is needed. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.ConcurrencyDemo">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
